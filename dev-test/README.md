# `dev-test`

Integration Test for `dev/` module.

This cannot be there, because the whole point of this is to test that its JAR is packaged correctly (with the HTML in `/browser/` in the JAR).

Originally motivated by https://github.com/google/adk-java/issues/302, where that was broken.

Michael Vorburger.ch, 2025-08-13
